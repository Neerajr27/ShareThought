<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="w-full min-h-screen bg-blue-200 p-10">
        <h5 class="tracking-tighter mb-2 text-black">Update Your Post!</h5>
        <form action="/update/<%= post._id %>" method="POST">
            <textarea class="block p-3 w-1/4 mt-4 resize-none bg-transparent border-2 border-black-800 outline-none rounded-md" name="content"><%=post.content%></textarea>
            <input type="submit" value="Update Post!" class="px-3 py-2 bg-yellow-500 rounded-md mt-2 text-black cursor-pointer hover:bg-yellow-600">
        </form>
    </div>
</body>
</html>